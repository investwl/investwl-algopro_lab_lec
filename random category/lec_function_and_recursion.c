#include <stdio.h>

int coba();

int main(){
    // kalo mau pass array as parameter,
    // bisa parameternya pointer atau nama_var[], amanan pointer
    // terus nnti di function pas mau olah, bisa misal
    /*
    for (int i = 0 ; i < x ; i++){
        nama_var[i] ...
    }

    nanti di int main, func(nama_var) KALO ARRAY
    kalo func(&nama_var) ITU BERLAKU UTK INT CHAR FLOAT DLL LAH
    

    */

    /*
    kalo 2d array
    dia paramternya array ke 2 harus ada nilai absolute, yg pertama terserah
    misal list[][10] atau list[10][10]
    */
}